pkgname="manjarofetch"
pkgver="1.0.0"
pkgrel="1"
pkgdesc="See informaations about your PC. Fork of zorinfetch"
arch=("x86_64")
license=("GPL")
source=("manjarofetch")
sha512sums=("SKIP")
package() {
  mkdir ${pkgdir}/bin/
  cp ${srcdir}/manjarofetch ${pkgdir}/bin/
  chmod +x ${pkgdir}/bin/manjarofetch
} 

 
